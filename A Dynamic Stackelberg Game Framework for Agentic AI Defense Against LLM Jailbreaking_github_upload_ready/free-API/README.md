# Omitted Internal Integration

The original `free-API` directory depended on a private internal gateway deployment and contained embedded credentials. Those scripts were removed from the public release.

Use the retained root-level experiment scripts for the public workflow. If you later decide to recreate the omitted gateway-based variants, do so locally with your own infrastructure rather than publishing those integrations here.
